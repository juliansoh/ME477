/*
 * Lab <number>
 * Author: <name>
 * Date: <date>
 * Description: <lab exercise description>
 */

/* includes */
#include <stdio.h>
#include <stdio.h>
#include "MyRio.h"
#include "T1.h"

/* prototypes */

/* definitions */

int main(int argc, char **argv)
{
	NiFpga_Status status;

    status = MyRio_Open();		    			// open FPGA session
    if (MyRio_IsNotSuccess(status)) return status;

    //my code here
    //printf("Hello World!\n");				// Print to console
    //printf_lcd("\fHello World 2!\n");		// Print to LCD display

    //Prompt user for floating-point number that is returned as a string (char[])
    //char *user_input = get_userinput();
    get_userinput();


	status = MyRio_Close();						// close FPGA session

	return status;

}

void get_userinput()
{
	char prompt_string[40] = "Enter a floating number:";
	double_in(prompt_string);
	//char input[] =
	return;
}
